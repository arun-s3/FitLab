const express = require('express')
const offerRouter = express.Router()
// const {createOffer, getAllOffers, updateOffer, deleteOffer, searchOffers,
//      getBestOffer, compareOffers} = require('../Controllers/offerController')
// const {isLogin, isLogout} = require('../Middlewares/Authentication')


// offerRouter.get('/', isLogin, searchOffers)
// offerRouter.post('/add', isLogin, createOffer)
// offerRouter.post('/list', isLogin, getAllOffers)
// offerRouter.post('/update/:offerId', isLogin, updateOffer)
// offerRouter.delete('/delete/:offerId', isLogin, deleteOffer)
// offerRouter.get('/bestOffer', isLogin, getBestOffer)
// offerRouter.post('/compare', isLogin, compareOffers)




module.exports = offerRouter